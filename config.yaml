# ── Server ────────────────────────────────────────────────────────────────────
server:
  host: 0.0.0.0
  port: 8000

# ── Project ───────────────────────────────────────────────────────────────────
project:
  directory: /home/jovyan/vast/kaiwen/track-mjx
  screen_session: train-vqvae      # auto-attach in Terminal tab (if running)
  claude_screen_session: claude     # screen session name for Claude tab
  claude_user: devuser              # su to this user before launching claude

# ── Clusters ──────────────────────────────────────────────────────────────────
clusters:
  topovnl-salk:
    host: 10.7.30.216
    port: 30988
    username: root

  vqmimic-salk-1:
    host: 10.7.30.114
    port: 30804
    username: root

  vqmimic-salk-2:
    host: 10.7.30.112
    port: 32348
    username: root

  vqmimic-salk-3:
    host: 10.7.30.112
    port: 31658
    username: root
