# ── Server ────────────────────────────────────────────────────────────────────
server:
  host: 0.0.0.0
  port: 8000
  # Origins allowed to call this backend cross-origin (e.g. a static frontend
  # hosted on GitHub Pages). Set to ["*"] to disable the allowlist.
  cors_origins:
    - http://localhost:8000
    - http://127.0.0.1:8000
    - https://kevinbian107.github.io

# ── Project ───────────────────────────────────────────────────────────────────
project:
  directory: /home/jovyan/vast/kaiwen/code2act    # file explorer root & Claude tab cwd
  claude_screen_session: claude-remote-access     # screen session name for Claude tab
  claude_user: devuser                            # su to this user before launching claude

# ── Clusters (Salk RunAI) ─────────────────────────────────────────────────────
clusters:
  topovnl-salk:
    host: 10.7.30.216
    port: 30988
    username: root

  vqmimic-salk-1:
    host: 10.7.30.114
    port: 30804
    username: root

  vqmimic-salk-2:
    host: 10.7.30.112
    port: 32348
    username: root

  vqmimic-salk-3:
    host: 10.7.30.112
    port: 31658
    username: root
